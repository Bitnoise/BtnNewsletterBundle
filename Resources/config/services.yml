services:
    btn.newsletter:
        class: Btn\NewsletterBundle\Model\NewsletterManager
        arguments:
            params: %btn_newsletter%
            formFactory: @form.factory
            em: @doctrine.orm.entity_manager

    btn.newsletter.twig:
        class: Btn\NewsletterBundle\Twig\NewsletterExtension
        tags:
            - { name: twig.extension }
        arguments:
            bc:   '@btn.newsletter'
            twig: '@twig'